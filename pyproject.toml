[project]
name = "self-healing-drone-agents"
version = "1.0.0"
requires-python = "==3.13.*"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "pydantic",
    "langgraph",
    "langchain",
    "langchain-core",
    "langchain-community",
    "python-dotenv",
    "sentence-transformers",
    "faiss-cpu"
]

[tool.setuptools]
packages = []

[tool.fastapi]
entrypoint = "api.main:api"