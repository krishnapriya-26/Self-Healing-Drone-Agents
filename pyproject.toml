[project]
name = "self-healing-drone-agents"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "langgraph",
    "langchain",
    "langchain-core",
    "langchain-community",
    "python-dotenv"
]